## 1. Update README.md

- [x] 1.1 Agregar sección "Script de utilidad (run.sh)" con descripción del script y modo interactivo
- [x] 1.2 Agregar tabla de comandos CLI con todos los comandos (`dc:up`, `dc:down`, `dc:status`, `dev`, `test`, `compile`, `clean`, `clean-test`, `coverage`)
- [x] 1.3 Actualizar "Inicio rápido" para usar `./run.sh dc:up` y `./run.sh dev` en lugar de comandos manuales
- [x] 1.4 Agregar ejemplo de flujo de trabajo típico (e.g., `./run.sh clean-test` para clean + tests)
- [x] 1.5 Verificar que el README se vea correctamente (formato markdown, tablas alineadas)
