services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: franky
      POSTGRES_USER: franky
      POSTGRES_PASSWORD: franky
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
